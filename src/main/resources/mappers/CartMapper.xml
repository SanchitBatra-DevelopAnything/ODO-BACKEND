<mapper namespace="com.yourpackage.mapper.CartMapper">

    <select id="findCartIdByMemberId" resultType="string">
        SELECT cart_id FROM cart WHERE member_id = #{memberId}
    </select>

    <insert id="insertCart">
        INSERT INTO cart (cart_id, member_id, cart_items)
        VALUES (#{cartId}, #{memberId}, CAST(#{cartItemsJson} AS JSON))
    </insert>

    <update id="updateCart">
        UPDATE cart SET cart_items = CAST(#{cartItemsJson} AS JSON)
        WHERE cart_id = #{cartId}
    </update>

    <delete id="deleteCartByMemberId">
        DELETE FROM cart WHERE member_id = #{memberId}
    </delete>

    <select id="getCartByMemberId" resultType="com.yourpackage.model.CartEntity">
        SELECT cart_id, member_id, cart_items FROM cart WHERE member_id = #{memberId}
    </select>

</mapper>
